services:
  - type: web
    name: bookkeeping-ai
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py   # 如果你的入口文件是 main.py
    autoDeploy: true
